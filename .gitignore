# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude/

# Large real-world SVG test corpus (~313MB): downloaded on demand via
# `npm run corpus:fetch` (see test/corpus/corpus.json). The manifest + fetch
# script are committed; the heavy .svg files are not.
/test/corpus/svg/

# Broad SVG smoke tier (W3C SVG 1.1 suite): fetched via `npm run smoke:fetch`.
/test/smoke/svg/
/test/smoke/.cache/

# Playwright
/test-results/
/playwright-report/
/.playwright/

# Supabase CLI local cache
/supabase/.temp/
